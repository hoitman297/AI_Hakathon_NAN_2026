# 개발 요약 — 2026-08-03

> 오늘 세션에서 진행한 백엔드/llm-proxy/프론트엔드 작업 정리. 커밋 여부와 무관하게 실제 작업 내용 기준.
> (이 파일은 하루 동안 여러 차례 갱신됨 — 최신본 기준으로 다시 정리함)

## 1. 백엔드 코드 복구
- 실수로 revert됐던 백엔드 초기 구현(`9659bc3 백앤드 개발 설정`)을 `git revert`로 재복구
  - 도메인 엔티티 30여 개, Repository, Service, Controller, DTO, DataSeeder 전체
  - `.gitignore` 충돌 1건 해결

## 2. 로컬 개발 환경 설정
- DB 스키마 생성 전략 결정: 로컬은 `DB_DDL_AUTO=update`(Hibernate 자동 생성), 그 외 기본값 `none`
- `backend/application-local.yml.example`, `llm-proxy/application-local.yml.example` 신규 생성 (README 온보딩 단계가 참조하던 존재하지 않던 파일)
- `dev-onboarding-guide.pdf` 기준으로 `.env`, `application-local.yml` 값 정합성 맞춤 (`MYSQL_PASSWORD`와 `DB_PASSWORD` 일치 등)
- 이후 팀 공유 Aiven DB로 전환 시 backend/llm-proxy 양쪽 접속 정보 통일

## 3. 백엔드 버그 수정 (자체 감사 기반)

### 3-1. 사보타주 다양성 부재
- `CulpritProfileRegistry`: NPC별 대상 풀을 `Target(location, subTarget)` 리스트로 구조화, 매일 밤 랜덤 선택(직전 밤과 다르게)
- `SessionService.generateNightSabotage`: 실제 낮 동선과 겹치는 목격 NPC(`witnessNpc`) 배정 로직 추가
- 5일간 사보타주 장소/대상이 매번 같았던 문제, 목격담 연결 안 되던 문제 해결

### 3-2. 게임 종료 조건 누락
- `SessionService.advanceDay`: 9일차에 고발 없이 `advance`만 호출하면 `current_day`가 무한 증가하던 버그 수정 → 9일차 도달 시 `BAD_ENDING`으로 강제 종료

### 3-3. 인가(Authorization) 부재 — IDOR
- 로그인 필수 정책으로 전환 (게스트 플레이 제거)
- `SessionOwnershipInterceptor` 신규: `/api/sessions/**` 전체에 로그인 토큰 검증 + 세션 소유권 검증 중앙화
- `ForbiddenException`(403), `WebMvcConfig`(인터셉터 등록), `GameSession.account` `nullable=false`로 변경
- `SessionController`에서 게스트 폴백 로직 제거

### 3-4. 트랜잭션 중 외부 HTTP 호출 타임아웃 없음
- `LlmProxyRestClientConfig`: connect 3초 / read 20초 타임아웃 설정
- `GlobalExceptionHandler`: `ResourceAccessException` → 504로 명확히 응답

### 3-5. 동시성 제어 없음
- `GameSession`, `PlayerStat`, `Affinity`, `InventoryItem`, `FarmPlot`, `FruitForageState`에 `@Version`(낙관적 락) 추가
- `GlobalExceptionHandler`: `ObjectOptimisticLockingFailureException` → 409로 응답

### 3-6. 입력 검증 부재
- `PlantRequest`/`HarvestRequest`/`ForageRequest`/`PurchaseRequest`/`AccuseRequest`/`SellRequest`/`UseItemRequest`/`DialogueRequest`에 Bean Validation 제약 추가 (`@NotNull`, `@Positive`, `@Min/@Max` 등)
- 해당 DTO를 받는 컨트롤러 8곳에 `@Valid` 추가
- 음수 수량 판매 시 골드/재고가 반대로 조작되던 버그 차단

### 3-7. 상점 아이템 효과 미반영
- 거짓말탐지기/돋보기 "1일 1회" 제한을 실제로 강제 (`GameSession`에 `lastLieDetectorUseDay`/`lastMagnifierUseDay` 추가)
- 이미 명확화된 단서를 돋보기로 재사용하던 버그 차단 (`ClueService.clarify`)
- 운동화 효과는 이동(move) API 자체가 없어 보류 (별도 설계 필요, `backend/TODO.md`에 기록)

### 3-8. CORS 미설정
- `WebMvcConfig.addCorsMappings()` 추가 (허용 origin은 `CORS_ALLOWED_ORIGINS` 환경변수로 설정 가능, 기본값 `http://localhost:5173`)
- `SessionOwnershipInterceptor`가 CORS preflight(OPTIONS) 요청을 인증 없이 통과하도록 수정 (안 그러면 preflight가 401로 막혀 CORS가 항상 실패)

## 4. llm-proxy 실제 LLM 연동
- Mock 응답이었던 `LlmService`를 Anthropic Java SDK(`com.anthropic:anthropic-java:2.34.0`) 기반 실제 Claude API 호출로 교체
- `AnthropicClientConfig`: `AnthropicClient` 빈 생성
- `GeneratedPersona`: 페르소나 생성 결과를 위한 구조화 출력(Structured Output) 스키마 — 대화용 LLM이 그대로 재료로 쓸 수 있게 배경 서사/동기/예시 대사까지 포함
- `generatePersona()`: Structured Output으로 NPC 기본 정보 → 상세 페르소나 확장 생성, 실패 시 대체 페르소나로 폴백
- `chat()`: 저장된 페르소나 + 대화 이력 기반 실시간 응답, 응답 속도를 위해 `thinking` 비활성화
- **`checkRefusal()` 추가**: Claude가 안전 정책상 응답을 거부(`stop_reason: "refusal"`)하면 명확한 예외로 걸러서 기존 폴백 로직으로 연결 (persona 생성/대화 응답 둘 다 적용)
- 모델은 `claude-sonnet-5`로 설정 (`LLM_MODEL` 환경변수, 비용/속도 이유로 opus-5에서 변경). 실제 Anthropic API 키도 `llm-proxy/application-local.yml`에 등록 완료

## 5. 게임 세이브/로드 기능 (신규, 기존 정규화 스키마와 독립)
- `game_save` 테이블: `account_id`(PK+FK), `save_data`(LONGTEXT JSON), `ending_state`, `updated_at`
- `GameSave`(Entity, `@OneToOne`+`@MapsId`로 계정과 1:1 연결), `GameSaveRepository`, `SaveRequest`/`SaveResponse`(DTO), `GameSaveService`(Jackson 수동 직렬화/역직렬화), `SaveController`(`/api/save`)
- 로그인 계정 기준으로 저장 (요청 당시 논의했던 "익명 UUID" 방식 대신 기존 보안 정책과 통일)
- 세이브 없으면 404 대신 신규 게임 기본값을 200으로 반환 (프론트 분기 단순화)

## 6. 프론트엔드
- `api/authToken.ts`: 로그인 토큰 localStorage get/set/clear
- `api/saveApi.ts`: `saveGame()`/`loadGame()` — Authorization 헤더 자동 첨부
- `game/autoSave.ts`: 일차(day) 전환 시점에만 저장 API를 호출하는 트리거 유틸 (`createDayChangeAutoSave`)

## 7. 로컬 빌드/실행 이슈 해결 (실제 검증 과정에서 발견한 버그들)
사용자 PC에서 `./gradlew build`를 실제로 돌려보면서 아래 4개의 진짜 버그를 순서대로 발견·수정함. **최종적으로 backend, llm-proxy 둘 다 `BUILD SUCCESSFUL`(테스트 포함, 실제 Aiven DB 연결 확인)까지 검증 완료.**

1. **Gradle Plugin Portal 색인 문제**: `org.springframework.boot:4.1.0` 플러그인이 Plugin Portal의 실제 "use API"에서 404 (사람이 보는 페이지엔 있는데 API 색인이 안 된 상태). → `settings.gradle`(backend/llm-proxy 둘 다)에 `maven { url 'https://plugins.gradle.org/m2/' }`를 `pluginManagement.repositories`에 직접 등록해서 우회
2. **`ObjectMapper` 빈 누락**: `spring-boot-starter-webmvc`가 이 Spring Boot 버전에서는 Jackson 자동 설정을 안 끌고 와서, `LlmService`(llm-proxy)와 `GameSaveService`(backend) 생성자 주입이 `NoSuchBeanDefinitionException`으로 실패. → 양쪽에 `JacksonConfig`(`@Bean ObjectMapper`) 추가
3. **backend에 `jackson-databind`가 컴파일 클래스패스에 아예 없었음**: llm-proxy는 Anthropic SDK가 우연히 Jackson을 끌고 와서 안 드러났던 문제. `spring-boot-starter-json` 추가로도 안 돼서, `jackson-databind`/`jackson-datatype-jsr310`을 `build.gradle`에 직접 명시해서 해결
4. **`GameSave.java`의 `@Comment` 위치 오류**: `@OneToOne @MapsId` 연관관계 필드에 `@Comment`를 붙이면 Hibernate가 "매칭되는 컬럼 없음"으로 거부. `@Id` 필드(`accountId`) 쪽으로 옮겨서 해결

## 8. 문서화
- `backend/TODO.md`: 추가 개발 필요 항목 정리 및 커밋 완료
  - 보조 사보타주 유형 미구현, 이동 API 없음, 랜덤 이벤트 콘텐츠 없음, 엔딩 스토리 콘텐츠 부족, 테스트 커버리지 없음, `game_save.ending_state` 미연동
- 이 파일(`DEV_LOG_2026-08-03.md`)을 `기타/3. 백앤드 로그/`로 이동/정리

## 아직 안 된 것 (현재 기준 최신 상태)
- `backend/TODO.md`의 2~7번 항목 (보조 사보타주 유형, 이동 API, 랜덤 이벤트/엔딩 콘텐츠, 테스트 커버리지, `game_save.ending_state` 연동) — 아직 미착수
- **오늘 변경된 backend/llm-proxy 파일 대부분 아직 커밋 안 됨** (`backend/TODO.md`만 커밋됨, `822dafc`). 단, 이제 실제 빌드 검증까지 끝났으니 커밋해도 안전한 상태
- 프론트엔드는 API 클라이언트 함수(`saveApi.ts` 등)만 준비된 상태 — 실제 화면(로그인/게임 UI)과 연결은 아직 안 됨
